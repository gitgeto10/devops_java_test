name: Java CI with Maven and MySQL

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-test:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}  
          MYSQL_DATABASE: ${MYSQL_DATABASE}
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping --silent" --health-interval=10s --health-timeout=5s --health-retries=5

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Load environment variables from .env
      run: |
        export $(cat .env | xargs)
        echo "Loaded environment variables from .env"

    - name: Wait for MySQL to be ready
      run: |
        for i in {1..30}; do
          if mysql -h 127.0.0.1 -P 3306 -u $DB_USER -p$DB_PASS -e "SELECT 1"; then
            break
          fi
          sleep 2
        done

    - name: Create schema
      run: |
        if [ -z "$DB_PASS" ]; then
          # Si le mot de passe est vide, ne pas utiliser l'option -p
          mysql -h 127.0.0.1 -P 3306 -u $DB_USER $MYSQL_DATABASE < schema.sql
        else
          # Si un mot de passe est défini, l'utiliser
          mysql -h 127.0.0.1 -P 3306 -u $DB_USER -p$DB_PASS $MYSQL_DATABASE < schema.sql
        fi

    - name: Build with Maven
      run: mvn -B package --file pom.xml

    - name: Run tests
      run: mvn test
      env:
        DB_URL: $DB_URL
        DB_USER: $DB_USER
        DB_PASS: $DB_PASS
